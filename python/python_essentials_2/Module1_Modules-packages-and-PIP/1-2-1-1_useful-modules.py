import math
for name in dir(math): #using dir() method to list all entities' names available in the module
    print(name, end="  /  ")

#***NOTE:*** can also run dir() directly in Python console With Out the for loop